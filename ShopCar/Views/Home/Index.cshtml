@model HomeViewModels

@{ViewData["Title"] = "ShopCar"; }
<h1 class="mb-2">Լավագույն մեքենաները</h1>


<div class="row mt-5 mb-2">
    @foreach (Car car in Model.FavCars)
    {
        @Html.Partial("AllCars", car);
    }
</div>




